# Frontend Environment Variables
# Copy to .env and fill in values

# API base URL — in dev, Vite proxies /api so this can be empty
VITE_API_URL=

# Backend URL for Socket.IO (use your Render URL in production)
VITE_SOCKET_URL=http://localhost:5000
